name: Test localgovdrupal/localgov_claro drupal-theme

on:
  push:
    branches:
      - '2.x'
  pull_request:
    branches:
      - '2.x'
  workflow_dispatch:

jobs:
  tests:
    uses: localgovdrupal/localgov_shared_workflows/.github/workflows/test-module.yml@1.x
    with:
      project: 'localgovdrupal/localgov_claro'
      project_path: 'web/themes/contrib/localgov_claro'
